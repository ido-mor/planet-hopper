---
description: Open the game in a browser tab after each completed request
alwaysApply: true
---

# Open game after request

Project rules: **AGENTS.md**. This file is Cursor-only workflow (Claude Code ignores it).

When you have **fully finished** handling the user's request (all edits done, no follow-up work left), open the game in the default browser so they can try it.

Run this command once at the end of your response, from the project root:

```bash
open "/Users/katerina/Documents/Cursor Projects/planet-hopper/index.html"
```

Do not run it in the middle of a multi-step task — only after the request is complete.
